language: java

jdk:
  - oraclejdk8

# See output of Gradle test
env:
  - TERM=dumb


# Stops Travis CI from making a virtual machine for each build.
sudo: false

# Skips install "gradle assemble"
install: true